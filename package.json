{
  "name": "monorepo",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=16",
    "pnpm": ">=7"
  },
  "scripts": {
    "commit": "git-cz",
    "pull": "git pull && cd .vscode && git pull && cd .."
  },
  "devDependencies": {
    "vite": "^3.1.3"
  }
}